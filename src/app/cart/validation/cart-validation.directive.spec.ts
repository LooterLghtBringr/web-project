import { CartValidationDirective } from './cart-validation.directive';

describe('CartValidationDirective', () => {
  it('should create an instance', () => {
    const directive = new CartValidationDirective();
    expect(directive).toBeTruthy();
  });
});
